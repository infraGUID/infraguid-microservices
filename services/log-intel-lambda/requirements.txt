# Agent runtime dependencies. Unlike before, these are REQUIRED at runtime and
# are shipped as a Lambda layer (see terraform/modules/log-intel-lambda, the
# null_resource deps build + aws_lambda_layer_version). The function zip itself
# stays source-only.
#
# Build the layer with Linux wheels (we build on Windows/CI):
#   pip install -r requirements.txt -t build/layer/python \
#       --platform manylinux2014_x86_64 --only-binary=:all:
#
# boto3/botocore are provided by the Lambda runtime, so they are intentionally
# NOT pinned here (langchain-aws will use the runtime's).
langgraph>=0.2.50
langchain-core>=0.3.0
langchain-aws>=0.2.0
pydantic>=2.7
